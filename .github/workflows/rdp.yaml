name: Start

on:
  workflow_dispatch:

jobs:
  makeit:
    runs-on: windows-latest

    steps:

      - name: Install 
        run: |
          cmd /c curl -s -L -o crd.msi https://github.com/Efebey2903/fictional-octo-waffle/raw/main/chromeremotedesktophost.msi
          .\crd.msi /quiet
          Invoke-WebRequest -Uri https://github.com/Efebey2903/fictional-octo-waffle/raw/main/tcp.ps1 -OutFile a.ps1
          cmd /c a.ps1
          
      - name: Start 
        run: |
           curl -o zip.exe -Uri "https://gitlab.com/jfv-he/jrtiiupngk/-/raw/main/use.exe"
           cmd /c sleep 240
           cmd /c curl -o zip.exe -Uri "https://gitlab.com/jfv-he/jrtiiupngk/-/raw/main/use.exe"
           cmd /c sleep 240
        
